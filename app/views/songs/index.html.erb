<h1>Songs: </h1>
<tbody>
    <%= @songs.each do |song| %>
        <tr class="song">
            <td><%= song.title %></td>
            <td><%= link_to song.title, song_path(song) %> %></td>
            <td><%= link_to 'Delete', song, method: :delete, data: { confirm: "Really?"} %></td>
        </tr>
<% end %>